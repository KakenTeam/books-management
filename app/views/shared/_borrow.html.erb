<%= form_for(current_user.borrowings.build, remote: true) do |f| %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :book_id, current_book.id %>
  <%= f.submit "Mượn", class: "btn btn-primary borrow-btn" %>
<% end %>